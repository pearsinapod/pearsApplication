package com.fb.pearsapplication.adapters;

public class exploreAdapter {//extends RecyclerView.Adapter<exploreAdapter.ViewHolder> {
/*
    private List<Group> mGroups;
    private Context context;


    @NonNull
    @Override
    public ViewHolder onCreateViewHolder(@NonNull ViewGroup parent, int viewType) {
        context = parent.getContext();
        View exploreView = LayoutInflater.from(context).inflate(R.layout.item_explore_group,parent,false);
        ViewHolder viewHolder =  new ViewHolder(exploreView);
        return viewHolder;
    }

    @Override
*//*
    public void onBindViewHolder(@NonNull exploreAdapter.ViewHolder holder, int position) {
        Group group = mGroups.get(position);

        holder.tvExploreName.setText(Group.get)
        //TODO define all descriptions and stuff
    }
*//*

    @Override
    public int getItemCount() {
        return mGroups.size();
    }
    class ViewHolder extends RecyclerView.ViewHolder {
        public ViewHolder(View itemView){
            super(itemView);

        }
    }*/
}
